## ✅ Чеклист выполнения Must-требований Sprint 3

| ID | Требование | Статус | Комментарий |
|----|------------|--------|-------------|
| **STR‑9** | Расширение структуры проекта: `database.py`, `repo.py`, `serial.py` | ✅ | Все модули созданы и интегрированы |
| **STR‑10** | `README.md` обновлён с инструкциями по `db init`, `repo serve`, примерами API | ✅ | Документация дополнена |
| **STR‑12** | `.gitignore` исключает `*.db`, `*.db-journal` | ✅ | Файлы БД не попадают в репозиторий |
| **CLI‑12** | Подкоманда `db init` создаёт схему SQLite (idempotent) | ✅ | Команда создаёт файл и таблицы, при повторном запуске не ломает |
| **CLI‑14** | `ca show-cert <serial>` выводит PEM сертификата по серийному номеру | ✅ | Реализовано, возвращает ошибку 404, если не найден |
| **CLI‑15** | `repo serve` запускает HTTP-сервер с параметрами `--host`, `--port`, `--db-path`, `--cert-dir`, `--log-file` | ✅ | Сервер работает на Flask, логирует запросы |
| **CLI‑17** | Команды `issue-cert` и `issue-intermediate` автоматически вставляют сертификаты в БД | ✅ | При успешной выдаче запись добавляется в таблицу `certificates` |
| **PKI‑13** | Генератор уникальных серийных номеров (64 бита, timestamp + CSPRNG) с проверкой уникальности через БД | ✅ | Функция `generate_unique_serial` гарантирует отсутствие дубликатов |
| **PKI‑14** | Автоматическая вставка в БД после подписания сертификата (serial, subject, issuer, даты, PEM, status) | ✅ | Все поля заполняются корректно |
| **PKI‑15** | Схема БД `certificates` соответствует спецификации (UNIQUE на serial_hex, индексы) | ✅ | Таблица создаётся скриптом `init_db` |
| **PKI‑17** | Ошибка вставки в БД → отказ выдачи сертификата (атомарность) | ✅ | При ошибке БД процесс прерывается, файлы не сохраняются |
| **DB‑1** | Функция `get_certificate_by_serial` используется в CLI и API | ✅ | Реализована, возвращает PEM или None |
| **DB‑2** | Функция `list_certificates` с фильтрацией по статусу | ✅ | Поддерживается в CLI `list-certs` |
| **REPO‑1** | HTTP-сервер (Flask) запускается через `repo serve` | ✅ | Используется встроенный сервер Flask (для разработки) |
| **REPO‑2** | `GET /certificate/<serial>` возвращает PEM (200) или 404 | ✅ | Проверка hex-формата, обработка ошибок 400 |
| **REPO‑3** | `GET /ca/root` и `GET /ca/intermediate` возвращают соответствующие CA сертификаты из `--cert-dir` | ✅ | Файлы читаются с диска, 404 при отсутствии |
| **REPO‑4** | `GET /crl` возвращает 501 Not Implemented | ✅ | Возвращается статус 501 с сообщением |
| **REPO‑6** | Логирование каждого HTTP-запроса (метод, путь, IP, статус) | ✅ | Логи пишутся с префиксом `[HTTP]` |
| **REPO‑8** | Обработка ошибок: неверный формат serial → 400, несуществующий serial → 404 | ✅ | Реализовано |
| **LOG‑6** | Логирование событий БД (инициализация, вставка, ошибки) | ✅ | Все операции логируются на уровне INFO/ERROR |
| **LOG‑7** | HTTP-логи отличаются от CLI-логов (префикс `[HTTP]`) | ✅ | Различные источники логов чётко различимы |
| **TEST‑13** | Выпуск 5+ сертификатов и проверка наличия записей в БД | ✅ | Тест `test_db_and_issuance` и `test_serial_uniqueness` покрывают |
| **TEST‑14** | CLI `list-certs` и `show-cert` работают корректно | ✅ | Проверено в тестах |
| **TEST‑15** | API `GET /certificate/<serial>` возвращает PEM, совпадающий с файлом | ✅ | Тест `test_repository_api` проверяет |
| **TEST‑16** | API `GET /ca/root` и `/ca/intermediate` возвращают ожидаемые PEM | ✅ | Проверено |
| **TEST‑19** | Неверный формат serial → 400 Bad Request | ✅ | Реализована проверка hex-строки |
